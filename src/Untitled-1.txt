<div className="min-h-screen bg-gradient-to-br from-black via-gray-900 to-gray-800 flex flex-col items-center py-6">
  <h2 className="text-3xl font-bold text-white mb-6">Vos Badges</h2>
  <div className="grid grid-cols-2 gap-4 w-full max-w-lg px-2">
    {badges.map(badge => (
      <div
        key={badge.id}
        className={`rounded-xl shadow-lg flex flex-col items-center p-4 transition ${
          badge.unlocked
            ? "bg-yellow-600/80 text-white"
            : "bg-black/40 text-gray-400"
        }`}
      >
        <div className="mb-2">{badge.icon}</div>
        <div className="font-bold text-center">{badge.name}</div>
        <div className="text-xs text-center mt-1">{badge.description}</div>
      </div>
    ))}
  </div>
</div>